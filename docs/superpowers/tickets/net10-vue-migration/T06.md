# T06: Reports + parity gate + cutover (delete old app)

- Risk: `high`
- Risk reasons: Irreversible deletion of the old src/backend/ and src/frontend/ codebase; Final parity gate that determines whether the migration is accepted
- Depends on: T05
- Owns: src/backend-net10/ShopApi/Controllers/ReportsController.cs, src/backend-net10/ShopApi/Models/ReportDto.cs, src/backend-net10/ShopApi.Tests/ReportsTests.cs, src/frontend-vue/src/views/ReportsView.vue, src/frontend-vue/src/stores/reports.ts, src/frontend-vue/src/api/reports.ts, tests/browser/reports.spec.ts
- Specification hash: a5646e10dfb81f9202e2e333efb64b3651932df00bc4f9d70725d4a189c1433e
- Approval hash: a5646e10dfb81f9202e2e333efb64b3651932df00bc4f9d70725d4a189c1433e

## Acceptance criteria

- `GET /api/reports` (authed) returns `200 { statusCounts: [{ status, count }], totalCustomers }`.
- `ReportsController` is `[Authorize]`; `ReportDto` mirrors the old `ReportViewModel`.
- The Vue `ReportsView` renders a status-counts table and bars (`el-progress` or CSS bars); `onMounted` calls `reportsStore.fetch()`.
- `reportsStore` exposes `data, fetch`; `reports.ts` Axios module calls `GET /api/reports`.
- `ReportsTests` xUnit passes. `smoke-content.sh` reports assertions pass. Playwright `reports.spec.ts` screenshots the reports page and verifies the bars render with a clean console.
- **Parity gate (human sign-off):** all four automated layers pass across all slices (xUnit + bash smoke + Vue build/typecheck + Playwright), AND a manual click-through of all five feature areas (login, dashboard, customer CRUD, interactions, reports) matches the old app. The user explicitly confirms parity.
- **Cutover:** only after parity is confirmed, delete `src/backend/`, `src/frontend/`, `tests/verify-*.ps1`, `src/backend/deploy.ps1`, and `src/backend/build-csc.ps1` in a single commit.

## Verification

```
dotnet test src/backend-net10/ShopApi.Tests
bash tests/smoke-auth.sh && bash tests/smoke-content.sh && bash tests/smoke-crud.sh
cd src/frontend-vue && npm run type-check && npm run build
npx playwright test tests/browser/
```